import React from 'react';

export default (props) => (
  <div className="popin">
    <div className="popin__one">
      {props.one}
    </div><div className="popin__two">
      {props.two}
    </div>
  </div>
);